<% if @status == 'success' %>
// idを指定してそのidに対してreplaceWithで置き換えている
$("#<%= @itinerary_spot.id %>").replaceWith("<%= j(render("tr", itinerary_spot: @itinerary_spot)) %>");
<% elsif @status == 'fail' %>
alert('error!');
<% end %>
$("#itinerary_spot-modal").modal("hide");